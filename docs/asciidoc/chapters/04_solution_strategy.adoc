ifndef::imagesdir[:imagesdir: ../images]

[[section-solution-strategy]]
== Solution Strategy

Die Lösungsstrategie basiert auf einer klaren Trennung zwischen der Steuerlogik (Python) und der visuellen bzw. physikalischen Darstellung (Unity).

---

=== Architekturelle Leitprinzipien

1. **Einheitliche Abstraktion**
   Der Benutzer soll nicht unterscheiden müssen, ob er die Simulation oder die reale Hardware steuert.
   Alle modusspezifischen Unterschiede (`virtual`, `real`, `both`) werden intern im System gekapselt.

2. **Entkopplung durch Netzwerkschnittstellen**
   Durch die Verwendung von UDP laufen Steuerlogik (Python) und Simulation (Unity) unabhängig voneinander.
   Dadurch wird verhindert, dass Performance-Schwankungen der 3D-Engine die Steuerlogik beeinflussen.

3. **Zustandsbasierte Kommunikation**
   Jedes UDP-Paket enthält den vollständigen Soll-Zustand (z. B. alle Gelenkwinkel).
   Dadurch kann ein verlorenes Paket durch das nächste automatisch korrigiert werden.

---

=== Entscheidungen zur Kommunikation

* **Warum UDP statt TCP?**
  In diesem System ist eine geringe Latenz wichtiger als eine garantierte Zustellung.
  Ein verlorenes Paket ist unkritisch, da kurze Zeit später ein neues Paket mit aktuellen Daten gesendet wird.
  TCP würde durch Wiederholungen (Retransmissions) zu Verzögerungen führen.

* **Warum JSON?**
  JSON ist einfach zu implementieren und gut lesbar, was das Debugging erleichtert.
  Da die Datenmenge pro Nachricht klein ist, überwiegen diese Vorteile gegenüber binären Formaten.

---

=== Schichtenmodell

[plantuml, target="diagram-solution-strategy", format="png"]
----
@startuml
skinparam rectangle {
    RoundCorner 15
}

actor Benutzer

rectangle "User Logic\n(Python Script)" as UserLogic
rectangle "Python API\n(Facade / Controller)" as API
rectangle "Transport Layer\n(JSON / UDP / Serial)" as Transport

rectangle "Unity Simulation" as Unity
rectangle "Realer Roboter" as Robot

Benutzer --> UserLogic
UserLogic --> API
API --> Transport

Transport --> Unity : UDP (5005/5006)
Transport --> Robot : Serial

@enduml
----

[cols="1,2", options="header"]
|===
| Schicht | Verantwortung

| **User Logic**
| Python-Skript des Benutzers. Enthält z. B. Bewegungslogik oder Szenarien.

| **API / Controller Layer**
| Zentrale Schnittstelle des Systems. Kapselt die Nutzung von `pymycobot`, validiert Eingaben und entscheidet, ob Befehle an Simulation, Hardware oder beide gesendet werden.

| **Transport Layer**
| Verantwortlich für die Übertragung der Daten (JSON, UDP, Serial).

| **Execution Layer**
| Führt die Befehle aus:
Simulation in Unity oder reale Ausführung auf dem Roboter.

|===

---

=== Strategie zur Kommunikationstrennung

Die Kommunikation zwischen Python und Unity erfolgt über zwei getrennte UDP-Ports. Dabei wird
Port 5005 für Roboterbefehle wie Gelenkbewegungen, Werkzeugbefehle und LED-Steuerung verwendet,
während Port 5006 für Befehle zur Verwaltung der Simulationswelt eingesetzt wird, beispielsweise
für `spawn`, `delete` oder `clear`. Diese Ports sind standardmässig festgelegt, können aber bei Bedarf angepasst werden.

Diese Trennung wurde bewusst gewählt, da beide Kommunikationsarten unterschiedliche Anforderungen
haben. Roboterbefehle treten häufig auf und sind zeitkritisch, da Bewegungen möglichst flüssig und
ohne Verzögerung in der Simulation sichtbar werden sollen. Befehle zur Weltverwaltung sind dagegen
seltener und eher diskrete Einzelaktionen. Durch die Aufteilung auf zwei getrennte Kanäle wird
verhindert, dass grössere oder unregelmässige Weltnachrichten die kontinuierliche Übertragung der
Roboterbefehle beeinflussen.

Zusätzlich verbessert diese Trennung die Struktur des Systems, da Robotersteuerung und
Weltverwaltung auch konzeptionell als zwei unterschiedliche Aufgabenbereiche behandelt werden.
Dadurch bleibt die Architektur klarer, leichter erweiterbar und einfacher zu warten.